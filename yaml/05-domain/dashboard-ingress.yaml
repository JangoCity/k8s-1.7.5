apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web.ityunv.com
  namespace: kube-system
spec:
  rules:
  - host: 192.168.61.100
    http:
      paths:
      - path: /
        backend:
          serviceName: kubernetes-dashboard
          servicePort: 80
