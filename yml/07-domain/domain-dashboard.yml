apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: dashboard-monitor-ingress
  namespace: kube-system
  labels:
    k8s-app: dashboard-monitor-ingress
spec:
  rules:
  - host: node75.ityunv.com
    http:
      paths:
      - backend:
          serviceName: kubernetes-dashboard
          servicePort: 80
